build.sh creates a Docker image for Ubuntu.

run.sh runs a Docker image created by build.sh.

Dockerfile is used by build.sh.  Do not run docker on it directly.

initialize-image.sh and initialize-container.sh are used in
building and running the image.  Do not run them directly.
